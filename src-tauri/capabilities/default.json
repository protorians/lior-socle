{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Default capability for the main window. Grants only the safe core permissions (window, app, event, menu, tray, path, resources, webview) and no filesystem, shell or network access.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
